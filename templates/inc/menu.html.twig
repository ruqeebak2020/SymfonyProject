<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <a class="navbar-brand" href="#"> {{ project_name }} </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ url('home') }}">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url('shop') }}">Shop</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url('providers') }}">Provider</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Contact</a>
        </li>
        <li>
          <!-- <div class="dropdown">
            <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Select Currency
            <span class="caret"></span></button>
            <ul class="dropdown-menu">
              <li> &nbsp;&nbsp;USD $ </li>
              <li> &nbsp;&nbsp;INR ₹ </li>
            </ul>
          </div> -->
          <select name="change_currency" id="change_currency"  class="form-control">

            {% if currency_code is defined %}
                <option value="USD" {{ (currency_code == 'USD') ? 'selected' : '' }} >USD</option>
                <option value="EUR" {{ (currency_code == 'EUR') ? 'selected' : '' }} >EUR</option>
                <option value="ZAR" {{ (currency_code == 'ZAR') ? 'selected' : '' }} >ZAR</option>
            {% else %}
                <option value="USD">USD</option>
                <option value="EUR">EUR</option>
                <option value="ZAR">ZAR</option>
            {% endif %}

          </select>
        </li>
      </ul>
    </div>
  </div>
</nav>

{% if errors is defined %}
    {% if errors != '' %}
        <br/><br/><br/>
        <div class="container">
        <div class="alert alert-danger">
        <strong>Errors!</strong> {{ errors }}
        </div>
        </div>
    {% endif %}
{% endif %}

<div id="loading_section" class="loading_section">
<div class="loader"></div>
<p>Updating price please wait...</p>
</div>
